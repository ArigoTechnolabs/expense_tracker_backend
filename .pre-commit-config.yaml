repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9  # use latest version
    hooks:
      - id: ruff
        args: ["--fix"]  # auto-fix where possible
      - id: ruff-format  # formats code (like Black)
